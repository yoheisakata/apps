# CLAUDE.md — kakeizan

単一 HTML ファイルの九九学習アプリ。ビルドなし・依存なし。
共通規約はルートの `CLAUDE.md` を参照。

- ページタイトルは「くくをおぼえよう」だが、ランチャー上の表示名は
  「かけざんクエスト」。名前を変えるならルート `index.html` と README も揃える。
- 読み上げは `speechSynthesis`。**進行状況は保存していない**（localStorage 不使用）。
  セーブ機能を足すならたしざんクエストの方式に合わせる。
- UI はひらがな中心の子ども向け日本語。
