# CLAUDE.md — tcpip

単一 HTML ファイルの TCP/IP 学習シミュレーター。ビルドなし。
共通規約はルートの `CLAUDE.md` を参照（Nunito・ダークテーマ）。

- アニメーションは JS のステップ実行モデル（再生 / 1ステップ / リセット）。
  ステップの定義を変えたら、途中リセットや連打でも状態が壊れないか確認する。
- 教材アプリなので、プロトコルの説明文はレイヤー名・ヘッダ名を正確に保つ。
- 変更後はブラウザで開き、ハンドシェイクとカプセル化/デカプセル化の両方を
  最後まで再生して確認する。
