## World Cup 2026 アプリ アーキテクチャ

大会は 2026-07-19 の決勝 (スペイン 1-0 アルゼンチン、延長) で終了済み。
このアプリは完全に静的なアーカイブ — ライブデータ取得は行いません。

```
┌─────────────────────────────────────────────────────────────┐
│                    ユーザーの iPhone / PC                      │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              ブラウザ (Chrome / Safari)                 │  │
│  │                                                       │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │         World Cup 2026 アプリ (JS)               │  │  │
│  │  │                                                 │  │  │
│  │  │  🇯🇵 日本  🏆 決勝T  📅 試合  📊 順位表  ...    │  │  │
│  │  └──────────┬──────────────────────────────────────┘  │  │
│  │             │                                          │  │
│  │     ┌───────▼──┐                                       │  │
│  │     │ 静的JSON  │  data/*.json (全104試合の最終結果)    │  │
│  │     │(唯一のデータ源)│                                    │  │
│  │     └──────────┘                                       │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│                    GitHub Pages (静的ホスティング)              │
│                                                             │
│  yoheisakata/apps リポジトリの main ブランチ                    │
│                                                             │
│  world-cup-2026/                                            │
│  ├── index.html          ← エントリーポイント                  │
│  ├── main.js             ← アプリ起動・タブ管理・静的データ読み込み │
│  ├── style.css           ← 全スタイル                        │
│  ├── data/                                                  │
│  │   ├── teams.json      ← 48チーム情報 (国旗・最終ランク等)   │
│  │   ├── groups.json     ← グループ A〜L の最終組み分け        │
│  │   ├── matches.json    ← 全104試合の最終結果                │
│  │   └── venues.json     ← 16会場の情報                      │
│  └── views/                                                 │
│      ├── country.js      ← チーム詳細ページ (🇯🇵 日本も共用)   │
│      ├── schedule.js     ← 📅 日程・結果                      │
│      ├── standingstab.js ← 📊 順位表                         │
│      ├── standings.js    ← 順位計算ロジック (共有)             │
│      ├── knockout.js     ← 🏆 決勝トーナメント表               │
│      ├── predict.js      ← Elo ベース予測エンジン (当時の参照実装) │
│      ├── rankings.js     ← 🥇 得点王ランキング                │
│      ├── cities.js       ← 🏟️ 開催都市マップ                  │
│      ├── world.js        ← 🌍 参加国マップ                    │
│      ├── teamlist.js     ← 👥 チーム一覧                      │
│      ├── matchmodal.js   ← 試合クリック時のモーダル             │
│      ├── livedata.js     ← Wikipedia スカッド読み込み + スコアラー名マッチング │
│      ├── wiki.js         ← Wikipedia 記事取得                 │
│      └── util.js         ← 共有ユーティリティ                  │
└─────────────────────────────────────────────────────────────┘


データの流れ (起動時):

  静的JSON読み込み (data/teams.json, groups.json, matches.json, venues.json)
  → 画面に即表示。ネットワーク取得はこれだけ。

  ※ かつての「Wikipedia優先 → openfootball フォールバック → 補完」ライブ更新
    パイプラインと FIFA ランキング用 Cloudflare Worker は、大会終了に伴い
    2026-07-19 に削除済み。最終結果はそのパイプラインを一度だけ Node で
    走らせて取得・マージし、data/*.json に焼き込んだ (決勝のみ手動確定 —
    フリーソースの反映が数時間遅れていたため)。当時の実装は git 履歴に残る。
```
