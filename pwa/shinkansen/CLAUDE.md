# CLAUDE.md — shinkansen

単一 HTML ファイルの新幹線路線マップ。ビルドなし。
共通規約はルートの `CLAUDE.md` を参照。

- 地図は **Leaflet**（CDN 読み込み）。路線・駅データは HTML 内にインラインで持つ。
- 子ども向けにひらがな中心の UI。
- **このアプリを変更したら `.app-version`（ヘッダー内）のバージョンを必ず
  上げること**。iPhone のホーム画面から開いたときに更新が届いたか確認する
  唯一の手段。
- 変更後はブラウザで開いて、地図表示と路線・駅のタップを確認する。
