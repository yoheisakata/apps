# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This repo has two distinct kinds of projects:

1. **Web apps** ("My Apps" — 旧アプリひろば) hosted on GitHub Pages — mobile-friendly, all-Japanese UI. The root `index.html` is the launcher/home screen. Category sections (each a `.section` with a `.section-title` and an icon `.grid`) were removed 2026-08-16 in favor of one flat grid, then reintroduced 2026-08-18 at the user's explicit request and split further the same day into three titled sections, in this order: **"ゲーム"** (boardgames/cardgames), **"学習"** (earth/suji-quest/moji-quest/rock-collections/pgquiz/awsquiz), and **"その他"** (shinkansen/tarot/タロットクイズ) last — do not add further section splits or reorder without asking. Since 2026-08-18, all web app folders live under **`pwa/<name>/`** (e.g. `pwa/earth/`) — only the launcher `index.html` and top-level docs stay at the repo root. Native macOS/iOS tools are unaffected and remain at the repo root.
2. **Native macOS/iOS tools** (`mynetworth/`, `myorganizer/`, `mygallery/`, `mytube/`, `mytube-ipad/`, `mygames/`, `mypass/`, `mymarkdown/`, `myslideshow-ipad/`, `kindle-transfer/`, `utilities/`) — personal-use local tools, built and run outside GitHub Pages, **not** referenced from the root `index.html`.

When editing, check which category a folder belongs to before assuming GitHub-Pages-style conventions (single HTML file, no build) apply.

Each app directory has its own `README.md` (user-facing, Japanese) and `CLAUDE.md` (dev guidance) — read the app's own docs before making changes there, and update them when behavior changes.

## Architecture

### Web apps (deployed to GitHub Pages)

Most are **single self-contained HTML files** (inline CSS + JS) in their own directory — no build step, no dependencies, no frameworks. Open any `index.html` directly in a browser to develop. Exceptions:

- **world-cup-2026/** — Multi-file vanilla app: `index.html` + `main.js` + ES-module views in `views/` + `style.css`, with static datasets in `data/*.json`. No build step. The tournament ended 2026-07-19 (final: Spain 1-0 Argentina) — the app is now a fully static archive; all 104 match results are baked into `data/*.json`. The old live-fetch pipeline (Wikipedia + openfootball fallback, a Cloudflare Worker proxying FIFA rankings) was removed — do not reintroduce it.
- **suji-quest/**（すうじくえすと）and **moji-quest/**（もじくえすと） — Multi-file vanilla apps, each `index.html` + `app.js` + `style.css` + `manifest.json` + `sw.js` (installable PWA with offline cache — bump `CACHE_NAME` in `sw.js` whenever cached assets change). Both are full duplicates of a single former app, `kids-learning-app/`（「まなびアプリ」）, split 2026-08-18 into two separate launcher tiles per user request — do not re-add `kids-learning-app/` as a combined app. `suji-quest/` exposes only たしざん (leveled, 5 levels) and くく/かけざん (speech-read multiplication tables via `speechSynthesis`) on its home screen; `moji-quest/` exposes only ひらがな and ローマ字タイピング. **Both copies still ship the full original `app.js`/`style.css`/`hiragana-strokes.js`/`audio/` codebase covering all 4 modes** — only the home-screen buttons (and thus which screens are reachable) were trimmed per app, deliberately, to avoid the risk of pruning interdependent shared JS (the star/reward system, audio playback, click-delegated screen routing). If touching shared logic in one, check whether the same fix applies to the other (they are not kept in sync automatically). Each has its own `localStorage` star key (`suji-quest-stars` / `moji-quest-stars`, forked from the original shared `manabi-stars` — progress does not carry over between them) and its own `CACHE_NAME` (`suji-quest-v1` / `moji-quest-v1`) so their offline caches don't collide. Absorbed (via the original `kids-learning-app/`) the former standalone `sansu/` app (たしざん levels + 九九) — do not re-add `sansu/` as a separate app either.
- **awsquiz/** — AWS Certified Solutions Architect – Professional (**SAP-C02**) 対策アプリ「AWS SAP 対策」: `index.html` (UI + logic) + `questions.js` (question bank) + `glossary.js` (用語集 data) + `manifest.json` + `sw.js` (installable PWA so it works offline on the iPhone). The data files are kept separate from `index.html` because they are large. No build step, no `fetch` — it still opens over `file://`. 202 questions across the exam's 4 domains, weighted to the real 26/29/25/20% split; a second axis of 15 技術分野 categories. Coverage was cross-checked against the official exam guide's task statements, including the newly added **Emerging Topics** section (responsible-AI controls — Bedrock Guardrails / AgentCore Identity / Step Functions human oversight), which is why a 生成AI(新領域) category exists. `SAP-C02-quiz.md` in the same folder is the user's own hand-written practice set; the questions whose `id` starts with `md-` were imported from it, so edits to one should be mirrored in the other. Five modes: 今日の練習 / 分野別クイズ / まちがい復習 / 模擬試験 (75問180分 or 40問95分, no per-question feedback, 1000点換算スコア) / フラッシュカード, plus 用語集 and 進捗 screens. The 用語集 is reachable from the quiz screen too (📖 button) and returns to the exact question you were on. Progress lives in `localStorage` (`awsquiz-v1`) as a per-question Leitner box (連続3回正解で習得ずみ) plus a daily log driving the exam countdown, daily goal ring and streak. **`sw.js` here is network-first on purpose** (cache-first would keep serving a stale `questions.js` after questions are added). See `awsquiz/CLAUDE.md` — it has the data schema, the `id`-is-a-save-key rule, and a validation one-liner to run after editing questions.

Static single-file apps: `earth`, `tarot` (`index.html` 占い + `quiz.html` クイズ), `shinkansen`, `pgquiz` (PostgreSQL 17以降向けのクイズ/フラッシュカード学習アプリ、104問・13カテゴリ), `rock-collections` (「いし図鑑」; カメラで撮影した石・鉱物を Claude API (Anthropic Messages API, ユーザーが自分の API キーを入力し `localStorage` に保存) で判定し、レア度つきの図鑑としてコレクションする子ども向けアプリ。外部 CDN の Font Awesome を読み込んでいる点が他の web アプリと異なる。manifest.json + sw.js でインストール可能な PWA — Kindle Fire 等のホーム画面に追加してオフラインでも起動できる。`sw.js` は awsquiz と同じ network-first(同一オリジンの GET のみ、api.anthropic.com など外部オリジンは素通し) — キャッシュ対象を変えたら `CACHE_NAME` を必ず上げる)。

Board games (`boardgames/shogi`, `boardgames/othello`, `boardgames/gomoku`, `boardgames/chess`, `boardgames/go`, `boardgames/mahjong`, `boardgames/diamond-game` — all under `pwa/boardgames/`) — all self-contained `index.html` apps, all independent from-scratch web implementations kept deliberately separate from `mygames`'s native Swift ボードゲーム tab (same relationship as `shogi/` vs. mygames's native 将棋). Each has 対AI対戦(with a difficulty picker)/2人対戦 modes except `mahjong`, which is inherently 4-player so it's 1人プレイ vs AI×3 instead. Each documents its own known rule simplifications in its `CLAUDE.md` — read that before assuming a rule is fully implemented. Since 2026-08-18 these 7 live as subfolders of **`pwa/boardgames/`**, one level below a picker screen at `pwa/boardgames/index.html` (a plain link grid, not a code merge — see `boardgames/CLAUDE.md`) so the root launcher only shows one "ボードゲーム" tile; the root `index.html` no longer links to any of them directly, and each game's `.home-link` points back to `../` (the picker, its immediate parent) instead of the root launcher.
- `shogi` — 将棋; self-implemented legal move generation, check/checkmate detection, negamax+alpha-beta AI. Known simplifications: 打ち歩詰め・千日手 not judged.
- `othello` — オセロ; corner/edge-weighted minimax AI (not pure material).
- `gomoku` — 五目並べ; intersection-based 15x15 board, freestyle rules (overlines count as a win), threat-heuristic + shallow alpha-beta AI.
- `chess` — チェス; full rules including castling/en passant/promotion. Known simplifications: no threefold repetition, no 50-move rule, no insufficient-material draws.
- `go` — 囲碁; 9x9 default (13x13 optional), simple ko (not superko), Chinese area scoring (not Japanese territory scoring with dead-stone negotiation), heuristic (non-search) AI.
- `mahjong` — 麻雀 (Riichi); full 136-tile wall, pon/ankan/ron/tsumo/riichi/furiten, core yaku set (riichi/tanyao/pinfu/yakuhai/tsumo/iipeikou/toitoi/chiitoitsu/honitsu-chinitsu/rinshan). Known simplifications: no chi, no daiminkan/shouminkan, no kokushi musou, no dora, no ippatsu/multi-ron, most mid-tier yaku and yakuman omitted (an unscored hand just doesn't register as a win rather than misscoring).
- `diamond-game` — ダイヤモンドゲーム (Chinese Checkers variant); cube-coordinate hex board (hexagon + 6 points, 121 holes), chained-jump move generation, progress-heuristic negamax AI. Currently 2-player only (4-/6-player not implemented).

`cardgames/` — トランプゲーム; unlike the board games above (which are grouped only navigationally via `boardgames/`), this is a single self-contained `index.html` that bundles **multiple** card games behind one home-screen game picker (not one app per game) — a true code merge, not just a link grid. Each game has its own setup screen (対AI対戦/2人対戦, plus a difficulty or other option) and keeps its own state object, sharing only deck/shuffle/card-rendering utilities (plus a shared ace-flexible `calcHandValue` between 21 and ブラックジャック). All 8 originally-requested games are implemented: 神経衰弱 (memory match), ババ抜き (old maid, 1 human + 2-3 CPU or 2人対戦), スピード (real-time sequential-card race), 大富豪 (shedding game with 8-切り), ７並べ (sevens), 21 (dealer-less multiplayer bust game), ポーカー (no-betting 5-card draw), ブラックジャック (CPU dealer, 1 or 2 human players sharing one dealer hand) — see `cardgames/CLAUDE.md` for the per-game rule simplifications (notably ババ抜き's/大富豪's fully-automatic cascading CPU turns, スピード's simplified stuck-pile auto-reset, and why 21 and ブラックジャック are deliberately separate rule sets despite both being blackjack-family games) before touching any game's logic.

> `learn-postgresql/` (pglite/WASM SQL lab) and `receipt/` (Claude API + Firebase レシート web app; the user's Firebase account was deleted — receipt management now lives in mynetworth's レシート tab) were removed from the repo; do not re-add references to them unless the folders come back.
>
> `mymusic/` (MyMusic) was removed 2026-09-02 at the user's explicit request — repo source, the installed `/Applications/MyApplications/MyMusic.app`, and its `~/Library/Application Support/MyMusic/` data (playlist, cache) were all deleted. Do not re-add it without asking.
>
> `myslideshow/` (MySlideshow, Mac版) was removed 2026-09-02 after its OneDrive-streaming + auto-play slideshow functionality was fully ported into **mygallery/** (new "ローカル"/"OneDrive" sidebar switch, an OneDrive link add/delete UI mygallery has that myslideshow deliberately never did, and a toolbar slideshow button — see `mygallery/CLAUDE.md`). Repo source, its installed app, and its `com.yoheisakata.myslideshow` preferences plist were all deleted. **`myslideshow-ipad/` (iPad version) was deliberately left untouched** and remains a separate, still-functioning app — do not remove it too without asking, and do not re-add `myslideshow/` (Mac版) without asking.
>
> `mydownloader/` (MyDownloader) was removed 2026-09-02 after its YouTube video/audio download functionality (`yt-dlp`/`ffmpeg` wrapper) was fully ported into **myorganizer/** as its「ダウンロード」pane — see `myorganizer/CLAUDE.md`. Repo source and the installed `/Applications/MyApplications/MyDownloader.app` were both deleted (it had no `~/Library/Application Support/` data dir to clean up). Do not re-add `mydownloader/` as a separate app without asking.

### Native macOS/iOS tools (not deployed to GitHub Pages)

- **mynetworth/**, **myorganizer/**, **mytube/**, **mygames/**, **mypass/** — SwiftUI apps built with **Swift Package Manager** (`Package.swift`, `Sources/`); **mygallery/** is the odd one out (plain Swift + AppKit, compiled with `swiftc` — no `Package.swift`). Shared conventions: `make-icon.swift` generates `AppIcon.icns`/`AppIcon.iconset/`, a build script produces a local ad-hoc-signed `.app` bundle. No App Store distribution, no CI. Install/update scripts differ per app:
  - All SPM apps: build script (bundle in place) + `./install.sh` (build + copy to `/Applications/MyApplications/`).
  - mynetworth: `./build_app.sh` or `./install.sh`. `build_app.sh` reads `appVersion` from `Sources/MyNetWorth/Main.swift` (single source of truth) into Info.plist, and bundles `2026_Sakata_支出表.md` as the 固定収支 tab's fallback.
  - myorganizer: `./build_app.sh` or `./install.sh`.
  - mytube: `./build_app.sh` or `./install.sh`.
  - mygames: `./build_app.sh` or `./install.sh`. See `mygames/CLAUDE.md`.
  - mypass: `./build_app.sh` or `./install.sh`.
  - mygallery: **no `install.sh`** — `./build.sh` (= `./build.sh install`) builds and copies to `/Applications/MyApplications/`; `./build.sh app` only builds the bundle in `build/`.
- **mynetworth/** (MyNetWorth — renamed from `networth`/NetWorth on 2026-08-12, together with its bundle ID `com.yoheisakata.mynetworth` and its data dir `~/Library/Application Support/MyNetWorth/`; the LaunchAgent became `com.yoheisakata.mynetworth-fetch`) specifics (v0.4.x, requires **macOS 26** via `Package.swift` — FoundationModels): tabs are メイン / 週 / 月 / 投資 / 固定収支 / レシート.
  - **The Keychain service string stays `com.yoheisakata.networth`** (`Keychain.swift`) — it is the lookup key of the already-stored SimpleFIN access URL, so renaming it would lose the token (same reasoning as MyPass's `PMBACKUP` magic).
  - `--fetch` CLI mode for headless data collection; `com.yoheisakata.mynetworth-fetch.plist` LaunchAgent runs it every morning (see [[networth-tracker]] memory for operational details).
  - 投資 tab overlays live quotes from Yahoo Finance's public chart API (`Quotes.swift`, no API key) on SimpleFIN's once-a-day holding values.
  - 固定収支 tab (`FixedBudget.swift`) renders `mynetworth/2026_Sakata_支出表.md` with a minimal Markdown parser — it reads the repo file at `~/github/apps/mynetworth/` directly (edit + 再読込 to update), falling back to the copy bundled at build time.
  - レシート tab (`Receipts.swift` + `ReceiptsTab.swift`) — Schedule C 向けレシート管理: Vision OCR + FoundationModels (on-device LLM) extraction; data lives in `~/Library/Application Support/Receipts/`. FoundationModels prompts must be in English (the model rejects prompts not matching the Apple Intelligence language setting). `ExpenseCategory` cases map to Schedule C Part II lines (8–27a) and their rawValues are persisted — never rename them.
- **kindle-transfer/** — Single Bash script (`kindle-transfer.sh`), no build. Uses `adb` to pull files from a Kindle Fire's SD card/internal storage over USB.
- **utilities/** — Standalone Python 3 / Bash scripts (not a packaged app) for a personal photo/video pipeline: backup organization (`backup-photos.sh`, `backup-videos.sh`, `sync-backups.sh`, `verify-photos.sh`), H.265 re-encoding (`encode_h265.py`), codec/container auditing (`check_video_codecs.py`), short-clip detection (`find_short_videos.py`). Run individually from the CLI; no shared entry point.
- **myorganizer/** — GUI front-end covering all of `utilities/`'s functionality (写真整理/動画整理/エンコード/誤配置修正/同期/短い動画検索 in a sidebar) plus a dependency-check pane. Deliberately **reimplements** the scripts' logic natively in Swift rather than shelling out to `utilities/` — the two do not stay in sync automatically; see `myorganizer/CLAUDE.md`. External tools (`ffmpeg`/`ffprobe`/`rsync`/`sips`/`mdls`/`yt-dlp`) are still invoked as subprocesses, not bundled. Also absorbed the former standalone `renamer/` app as its「リネーム」pane (rule-based batch renaming), the former standalone `cleanmac/` app as its「キャッシュ掃除」/「アプリ削除」panes (trash-only cache/app cleanup — the same `cleanmac/`-derived「重複写真」pane was later removed, see `myorganizer/CLAUDE.md`), the former standalone `omoide/` app as its「まとめ動画」pane (clips a kids'-video folder into one movie with title cards + BGM via ffmpeg — see `myorganizer/CLAUDE.md`), and the former standalone `mydownloader/` app as its「ダウンロード」pane (YouTube video/audio downloads via `yt-dlp`/`ffmpeg`, since 2026-09-02 — see `myorganizer/CLAUDE.md`) — do not re-add `renamer/`, `cleanmac/`, `omoide/`, or `mydownloader/` as separate apps.
- **mytube/** — Regular windowed app (standard SwiftUI `App`/`WindowGroup`, not menu-bar-resident — playback doesn't need to continue after the window closes). A video player with a YouTube-like look: **multiple** local folders can be open at once (each recursively scanned, its folder tree shown in the sidebar), and since 2026-08-04/05 two remote sources join the same library — OneDrive 共有リンク and YouTube playlists — both cached locally by `Core/DownloadStore.swift` (different download paths and post-cache handling for each; see `mytube/CLAUDE.md`). The sidebar groups them as ローカル / OneDrive / YouTube. A thumbnail grid (auto-generated + disk-cached frame per video, `~/Library/Caches/MyTube/thumbnails/`) is the home feed; `ContentView` swaps `HomeVideosView` for `PlayerPaneView` (`AVKit.VideoPlayer`) on `selectedVideo`, with an "up next" list that autoplays sequentially. Open folders persist as plain `UserDefaults` path strings (`Settings.openLocalFolders` — no security-scoped bookmark needed since the app isn't sandboxed). `Package.swift` must keep `linkerSettings: [.linkedFramework("AVKit")]` — without it the installed `.app` aborts at launch. See `mytube/CLAUDE.md` for the async thumbnail-cache design and the autoplay-closure-staleness pitfall.
- **mytube-ipad/** (MyTube Pad — bundle ID `com.yoheisakata.mytubepad`, added 2026-08-26) — iPadOS counterpart of mytube, deliberately scoped down to **OneDrive 共有リンク playback only** (no local folders, no YouTube — both blocked or not worth the complexity under iOS's app sandbox, see `mytube-ipad/CLAUDE.md`). **The only Xcode-project app in this repo** — every other SwiftUI native app is SPM-only; this one needs a real `.xcodeproj` to produce an installable iOS `.app`, generated from `project.yml` via `xcodegen` (`brew install xcodegen`; the `.xcodeproj` itself is gitignored, regenerate with `xcodegen generate` after pulling or editing `project.yml`). `Sources/MyTubePad/OneDriveShareClient.swift` is a near-verbatim port of mytube's client — it works unmodified on iOS because it's pure Foundation/`URLSession`, and crucially a *native* app (unlike a browser page) can set the `Origin`/`Referer` headers Microsoft's internal API requires. No install script — see `mytube-ipad/README.md` for the free-Apple-ID / 7-day-resign install flow via Xcode.
- **myslideshow-ipad/** (MySlideshow Pad — bundle ID `com.yoheisakata.myslideshowpad`, added 2026-08-30) — iPadOS app that streams photos/videos from OneDrive 共有リンク(s) and auto-plays them continuously, fullscreen, TV-style. Originally ported from a Mac sibling app `myslideshow/` (following the same porting pattern as mytube → mytube-ipad); that Mac app was removed 2026-09-02 after its functionality was folded into **mygallery/** (see below and `mygallery/CLAUDE.md`) — myslideshow-ipad itself is unaffected and still a fully independent, working app, just without a Mac-native sibling anymore. `Core/OneDriveMediaClient.swift` is pure Foundation/`URLSession`, ported near-verbatim from mytube's `OneDriveShareClient.swift`. What's iPad-specific (no Mac window-management concepts apply here): `NSEvent` keyboard monitoring became SwiftUI's `.onKeyPress` (iOS 17+); `AVPlayerView` (AppKit) became `AVPlayerViewController` wrapped in `UIViewControllerRepresentable` (same pattern mytube-ipad used); `Toggle(.checkbox)` (no iOS equivalent) became a custom capsule chip button. See `myslideshow-ipad/CLAUDE.md` for the full file-by-file diff against the (now-removed) Mac version.
- **mygames/** (MyGames — renamed from `emulator`/RetroGames on 2026-08-12, together with its bundle ID `com.yoheisakata.mygames` and its data dir `~/Library/Application Support/MyGames/`) — NES/SNES emulator frontend using the libretro API (loads `.dylib` cores at runtime via `dlopen`). Tabs: ライブラリ / ROM / コントローラー / ボードゲーム. Also absorbed the former standalone native `boardgames` app (将棋・チェス・オセロ・囲碁・五目並べ・麻雀・ダイヤモンドゲームの7種、AI対戦あり) as its「ボードゲーム」タブ — do not re-add that native app as a separate root-level app. (Unrelated name collision: `pwa/boardgames/` is a *different*, web-only picker screen — see below — that groups the still-separate `shogi`/`chess`/`go`/`othello`/`gomoku`/`mahjong`/`diamond-game` web apps; it is not the native app described here.) See `mygames/CLAUDE.md`.
- **mygallery/** (MyGallery — renamed from `photo-gallery`; bundle ID `com.yosakata.mygallery`) — Photos.app-like browser for a local folder tree, **not** an SPM package: `Sources/main.swift` + (since 2026-09-02) three companion files (Swift + AppKit, no WKWebView, no dependencies) compiled together by `build.sh`, which also writes the Info.plist inline and reads the version from the `VERSION` file. Nothing is imported into a library — the chosen root folder is scanned in place. Features: thumbnail grid + full-size viewer, sort orders including a Vision-based blur/quality score, filters (date range / 人物あり・なし / イラスト・写真, all on-device Vision), 重複検出 (⇧⌘D) with four match levels from exact SHA-256 to dHash fuzzy matching, and rotation that re-writes the original file. Deletions go to the Trash (⌘⌫, no confirmation, restorable). **Video playback (2026-08-21)**: MP4/MOV/M4V files are scanned alongside photos and play in the viewer via `AVPlayerView` (grid thumbnail = a frame grabbed with `AVAssetImageGenerator` + a ▶ badge); rotation/重複検出/人物・種類フィルター/画質順ソート are all local-file-only (Vision/CGImageSource-based) and simply exclude or safely reject video/OneDrive items rather than applying to them. The dup-detection logic was ported from myorganizer's old「重複写真」pane, which has since been removed from myorganizer — mygallery is now the only place it lives. **OneDrive streaming + slideshow (2026-09-02)**: absorbed from the now-removed `myslideshow/` (see above) — a sidebar「ローカル」/「OneDrive」switch lets you browse OneDrive 共有リンク(s) (with an add/delete-link UI myslideshow itself deliberately never had), and a toolbar button starts an auto-play slideshow (photo timer, video-plays-through, shuffle, time limit, fullscreen-only — no windowed/PIP modes, no highlight-playback) over whatever's currently in the grid, local or OneDrive. `Sources/OneDriveMediaClient.swift`/`Sources/OneDriveSidebarViewController.swift`/`Sources/SlideshowController.swift` — see `mygallery/CLAUDE.md` for the full design (notably the `PhotoItem.cacheKey`/`isVideo`-as-stored-field changes needed to support two very different item sources, and the OneDrive scan performance pitfalls carried over from myslideshow's own hard-won lessons).
- **mypass/** (MyPass — renamed from `passman`/PassMan on 2026-08-12, together with its bundle ID `com.yoheisakata.mypass` and its data dir `~/Library/Application Support/MyPass/`) — SwiftUI password manager. Everything is stored as one encrypted blob (`vault.dat`); the master password derives a KEK via PBKDF2-HMAC-SHA256 (600k iterations, CommonCrypto — `CryptoStore.swift` explains why Argon2id was not used despite `DESIGN.md`), which unwraps a DEK used for AES-256-GCM. Touch ID unlock stores the DEK in the Keychain (`BiometricStore.swift`). **The `.passmanbackup` extension and its `PMBACKUP` magic keep the old name on purpose** — they are file contents, so renaming them would break every previously exported backup (`VaultFile.swift`); `*.passmanbackup` is gitignored since backups are personal data.
- **mymarkdown/** (MyMarkdown — bundle ID `com.yosakata.mymarkdown`) — like mygallery, **not** an SPM package: `Sources/main.swift` + `Sources/AppShared.swift` (Swift + AppKit + `WKWebView`, no runtime networking) compiled directly by `build.sh`. Routes opened files by extension: `.md`/`.markdown`/`.mdown`/`.mkd` open as a live-reloading markdown **preview** (rendered via vendored marked.js + GitHub markdown CSS + highlight.js under `Resources/vendor/`, Mermaid lazy-loaded), everything else opens in a built-in plain-text **editor** (the former standalone "mini editor", merged in here — Find/Replace, fonts, zoom, word wrap, `.log` severity coloring). Live reload uses a `DispatchSource` file watcher plus a 0.5s mtime poll fallback (to survive atomic saves that replace the inode), re-injecting content via `evaluateJavaScript` while preserving scroll **ratio**. Theme is System/Light/Sepia/Dim/Dark via a `data-theme` attribute in `Resources/template.html`, shared across preview and editor tabs. Multiple files open as tabs; ⌘N opens a new empty document.

## Build / Dev Commands

Web apps need no build — open the HTML file in a browser. There are no tests anywhere in this repo.

### SwiftUI/SPM native apps (mynetworth, myorganizer, mytube, mygames, mypass)

```bash
cd <app>
swift build         # compile check (verification method — see below)
# Build the .app bundle:
./build_app.sh      # every SPM app
# Install/update in /Applications/MyApplications:
./install.sh        # every SPM app (build + copy to /Applications/MyApplications)
```

mygallery and mymarkdown are not SPM packages — use `./build.sh` there (see their entries above); `swift build` does not apply to them.

mytube-ipad and myslideshow-ipad are the iOS apps and use neither SPM nor `build.sh` — each is an Xcode project generated from its own `project.yml` via `xcodegen generate` (see `mytube-ipad/CLAUDE.md` / `myslideshow-ipad/CLAUDE.md`). `swift build` doesn't apply; for a compile check without needing Xcode's iOS platform component installed, type-check with e.g. `xcrun -sdk iphoneos swiftc -target arm64-apple-ios17.0 -typecheck Sources/MyTubePad/*.swift Sources/MyTubePad/Views/*.swift` (swap in `Sources/MySlideshowPad/{*.swift,Core/*.swift,Views/*.swift}` for that app). Actual builds/installs to a physical iPad happen in Xcode itself (user-driven — the "don't launch GUI apps" rule below is about this repo's SPM apps, not about the user's own Xcode run/install flow).

**GUI アプリを起動しないこと**: `swift run`・`open <App>.app`・`.build/debug/<App>` の直接実行など、ウィンドウが開く形での目視確認は禁止(permissions の deny ルールでもブロック済み)。検証は `swift build` のコンパイル確認まで。アプリの起動・目視確認はユーザー自身が行う。例外: `MyNetWorth --fetch` のようなヘッドレス CLI モードは実行してよい。

## Conventions

### Web apps
- **バージョン表示は必ず上げる。** ランチャーから開く各アプリのトップ画面（最初に出る画面）に `v1.0.0` 形式のバージョンを小さく出している。**そのアプリのファイルを変更したら、同じコミットで必ずバージョンを上げること**（軽微な修正 = パッチ、機能追加 = マイナー）。iPhone のホーム画面に登録した状態では「更新が届いたか」を確認する手段がこれしかないため、省略しない。置き場所は各アプリの `.app-version`（earth のみ固定表示の `#app-version`）:
  - **表示位置は必ず画面の上部**（タイトル/サブタイトルのすぐ下。earth のみ右上に固定表示の `#app-version`）。下端に置くとスクロールしないと見えないため、下には戻さないこと（パスはいずれも `pwa/` 配下）: `earth/index.html`（右上固定）/ `shinkansen/index.html`（ヘッダー内）/ `tarot/index.html`（ヘッダー内）/ `tarot/quiz.html`（イントロのサブタイトル下）/ `suji-quest/index.html`・`moji-quest/index.html`（タイトル下）/ `pgquiz/index.html`・`awsquiz/index.html`（`.app-sub` の下）
  - suji-quest/moji-quest は cache-first の SW なので、バージョンを上げるときは `sw.js` の `CACHE_NAME` も一緒に上げる（上げないと iPhone に届かない）。
  - ルート `index.html`（ランチャー）にはバージョン番号ではなく**更新日時**を出す。ヘッダーのサブタイトル（旧「あそびたいアプリをえらんでね」）が `更新 YYYY-MM-DD HH:MM:SS`（秒まで。同じ日に何度も更新するので日付だけに戻さないこと）。**値は読み込み時に自動計算する** — 各アプリのリンク先を `HEAD`（`cache:'no-store'`）で引いた `Last-Modified` と、`document.lastModified`（index.html 自身）のうち**新しい方**を表示する。したがって通常は手で直す必要はない。HTML に直書きしてある時刻は `fetch` が使えないとき（`file://`・オフライン）のフォールバックなので、`index.html` を触ったついでに現在時刻（`date "+%Y-%m-%d %H:%M:%S"`）へ直しておくとよい。
  - この更新日時はタップできる（ルート `index.html` 末尾の `<script>`）。押すと `?t=<現在時刻>` を付けて読み込み直し、その状態のときだけ各アプリへのリンクにも同じ `t` を伝播させて、HTTP キャッシュを避けて最新を取りに行く。ホーム画面に登録した web アプリには「引っぱって更新」が無いための代替手段。**通常時にリンクへ `t` を付けてはいけない**（PWA のオフラインキャッシュを外してしまい、suji-quest/moji-quest がオフラインで動かなくなる）。
  - world-cup-2026 は従来どおり `APP_VERSION` を使う。
- Dark gradient themes and CSS custom properties for colors; several apps (pgquiz) use the Nunito font (Google Fonts). **ルート `index.html`（ランチャー）だけは「ダークセピア」**（茶〜琥珀の暖色グラデ、`#372a1c`→`#251d15` + 暖色の ambient blob、文字は `#f2e6d3` 系、`theme-color` も合わせてある）— 真っ暗な配色は暗すぎるという指摘で 2026-08-13 に変更したので、黒基調に戻さないこと。各アプリ側は従来どおりそれぞれのダークテーマのまま。
- Mobile-first: `viewport` meta with `user-scalable=no`, touch-optimized interactions.
- State persistence via `localStorage` where it matters: world-cup-2026 stores the theme (`wc2026-theme`); suji-quest/moji-quest each persist their own star count (`suji-quest-stars`/`moji-quest-stars`).
- Icons are emoji or inline SVG data URIs — no external image assets.
- world-cup-2026 has two version knobs to bump on release: `?v=N` cache-busters on JS/CSS imports and `APP_VERSION` in `main.js` (shown in the header). The old `LIVE_CACHE_KEY` knob is gone with the live-fetch pipeline; `main.js` only keeps a one-time cleanup that purges the leftover live-era `localStorage` keys.

### Native macOS apps
- **App names use the `My〜` prefix** (MyGallery / MyGames / MyMarkdown / MyNetWorth / MyOrganizer / MyPass / MyTube / MyTube Pad / MySlideshow Pad — as of 2026-08-12 no app was left without the prefix; MyMarkdown, MyTube Pad, and MySlideshow Pad joined later as new apps already following the convention). When renaming one, change all of: directory name, SPM target + `Sources/<Target>/` (or, for non-SPM apps like mygallery/mymarkdown, just the `build.sh` binary/display names), product/executable name, `.app` display name, bundle ID, `~/Library/Application Support/<name>/` data dir (mv the existing one to carry data over), build/install scripts, README + CLAUDE.md, and the old `/Applications` copy (trash it). See the [[app-rename-my-prefix]] memory for the full checklist.
- Installs go to **`/Applications/MyApplications/`**, not `/Applications` directly (自作アプリをまとめるため).
- Deletions always go to the Trash (`FileManager.trashItem`), never a hard delete — see myorganizer's キャッシュ掃除/アプリ削除 panes.
- Secrets/credentials go in Keychain, never committed to the repo — see mynetworth's SimpleFIN token handling.
- Each app ad-hoc signs on local build; there's no shared signing identity or notarization.

### Repo-wide
- Commit messages are in Japanese.

## Deployment

- **Web apps**: GitHub Pages from the `main` branch. No CI/CD — pushing to `main` deploys automatically. Nothing is deployed anywhere else (the world-cup-2026 Cloudflare Worker was removed along with the live-fetch pipeline).
- **Native macOS apps**: never deployed via GitHub Pages. Each is built and installed locally to `/Applications/MyApplications/` via `./install.sh`(mygallery のみ `./build.sh`。自作アプリはこのサブフォルダにまとめている). Re-run the install step after pulling changes to update the installed copy.

## Updating the Launcher

Applies to **web apps only** — native macOS tools are never added to the launcher. New web app folders go under `pwa/<name>/`. When adding/removing a web app, update root `index.html`:
1. Add an `<a class="app {name}" href="pwa/{name}/">` entry to a `.grid` — "ゲーム" for games, "学習" for study/quiz/educational apps, or "その他" for anything that doesn't fit either (see above; don't add further section splits without asking).
2. Add a `.app.{name} .icon-wrap` CSS rule with a gradient background and box-shadow.
3. Set `data-added="YYYY-MM-DDTHH:MM:SS"`（追加した日時・ローカル時刻）on the `<a class="app">`. 末尾の `<script>` がこの日時を見て、**追加から24時間だけ** `.new-badge`（NEW）を動的に付ける。バッジを HTML に直書きしないこと（期限切れのものが一瞬表示されてしまう）。時刻を省いて日付だけ書いた場合は 00:00:00 とみなされる。

A new **board game** does not get its own root-launcher entry, and its folder goes under `pwa/boardgames/<name>/` (not directly under `pwa/`) — add it to `pwa/boardgames/index.html`'s picker grid instead (see `pwa/boardgames/CLAUDE.md`).
